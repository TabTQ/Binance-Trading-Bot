# Zerodha Trading Bot Dependencies

# Core Dependencies
kiteconnect>=4.0.0           # Zerodha Kite Connect API
pandas>=1.3.0
numpy>=1.20.0
TA-Lib>=0.4.24

# Optional Dependencies (bot will work without these)
# python-dotenv>=0.19.0  # For .env file support (optional - uses os.environ if not installed)

# To install all dependencies including optional:
# pip install python-dotenv>=0.19.0

# Note: TA-Lib requires system-level installation
# Ubuntu/Debian:
#   sudo apt-get update
#   sudo apt-get install build-essential wget
#   wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
#   tar -xzf ta-lib-0.4.0-src.tar.gz
#   cd ta-lib/ && ./configure --prefix=/usr && make && sudo make install
# macOS: brew install ta-lib
# Windows: Download from https://www.ta-lib.org/hdr_dw.html

# Kite Connect Setup:
# 1. Create account at https://kite.trade/
# 2. Subscribe to Kite Connect API
# 3. Get API Key and Secret from developer console
# 4. Generate access token via login flow
